#include<stdio.h>
 int main(){
  int path, save;

 printf("Você esta na sala 1, digite 1 ou 2:");
 scanf(" %d", &path);
    save=1;

if(path ==1){
printf("Sala 2, digite 1 ou 2:");
scanf(" %d", &path);
        save=save + 1;
} 
else if(path==2){
printf("Sala 3, digite 1 ou 2:");
scanf(" %d", &path);
        save=save + 2;
}
    if(save==2 && path==1){
        printf("Sala 3,digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+1;
        }
    else if(save==2 && path==2){
        printf("sala 4,digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+2;
    }
    if(save==3 && path==1){
        printf("Sala 4, apenas 1 caminho 1:");
        scanf(" %d", &path);
        save=save+1;
    }else if(save==3 && path==2){
        printf("sala 5,digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+2;
    }
    if(save==4 && path==1){
        printf("Sala 5, digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+1;
    }
    if(save==5 && path==1){
        printf("Sala 6, digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+1;
    }else if(save==5 && path==2){
        printf("sala 7,digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+2;
    }
    if(save==6 && path==1){
        printf("Sala 6,apenas um caminho 2:");
        scanf(" %d", &path);
        save=save+2;
    }
    if(save==7 && path==1){
        printf("sala 8,digite 1 ou 2:");
        scanf(" %d", &path);
        save=save+1;
    }else if (save==7 && path==2){
        printf("Sala 9, voce conseguiu parabens!!!");
        save=save+2;
    }
    if(save==8 && path==1){
        printf("Sala 9, voce conseguiu, parabens!!!");
        scanf(" %d", &path);
        save=save+1;
    }else if(save==8 && path==2){
        printf("portal....");
        scanf(" %d", &path);
        save=save+2;
    }

    return 0;
 }
